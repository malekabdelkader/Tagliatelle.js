/**
 * 🍝 Root Routes Config
 * 
 * This config applies to ALL routes.
 * Use JSX components to configure!
 */

import { Logger } from 'tagliatelle';

export default () => (
  <>
    <Logger level="info" />
  </>
);
